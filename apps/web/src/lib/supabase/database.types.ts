// AUTO-GENERATED PLACEHOLDER
// Replace with the real output of:
//   supabase gen types typescript --project-id <your-project-ref> > src/lib/supabase/database.types.ts
// Run this after applying the migrations in /supabase/migrations so every
// table, enum, and RPC (e.g. record_score_event) gets full type safety.

export type Database = {
  public: {
    Tables: Record<string, unknown>;
    Views: Record<string, unknown>;
    Functions: Record<string, unknown>;
    Enums: Record<string, unknown>;
  };
};
